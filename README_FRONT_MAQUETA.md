# Front-KATZE - Maqueta Components

Esta branch contiene la conversión de la maqueta KATZE a componentes React + Tailwind.

Run locally:

```
npm install
cp .env.example .env.local
npm run dev
```

Env:
- VITE_API_URL=http://localhost:5000/api/v1